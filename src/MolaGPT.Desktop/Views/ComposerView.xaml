<UserControl x:Class="MolaGPT.Desktop.Views.ComposerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:MolaGPT.ViewModels;assembly=MolaGPT.ViewModels"
             xmlns:views="clr-namespace:MolaGPT.Desktop.Views"
             d:DataContext="{d:DesignInstance vm:ComposerViewModel}"
             mc:Ignorable="d"
             Background="Transparent">

    <!-- Floating composer card. The layout uses a compact five-row grid:
         row 0 = attachment chips +
         optional file count / row 1 = action toolbar (附件/推理/推理强度/网络) /
         row 2 = image options / row 3 = textarea / row 4 = action bar (hint, send/stop). -->
    <Border Background="{DynamicResource Brush.Bg.Elevated}"
            CornerRadius="{DynamicResource Radius.Xl}"
            Padding="14,10"
            SnapsToDevicePixels="True">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border.Strong}" />
                <Setter Property="BorderThickness" Value="1.5" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsImageGenerationMode}" Value="True">
                        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Primary}" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Row 0: attachment cards. Header row shows count + clear-all;
                 below it is a horizontal wrap of cards. Image cards expose a 32×32 thumbnail
                 and open ImagePreviewWindow on click; non-image cards show a
                 typed icon. The whole strip collapses when Attachments is empty. -->
            <Border Grid.Row="0"
                    Background="{DynamicResource Brush.Bg.Tertiary}"
                    BorderBrush="{DynamicResource Brush.Border}"
                    BorderThickness="1"
                    CornerRadius="{DynamicResource Radius.Sm}"
                    Padding="8,5"
                    Margin="0,0,0,6"
                    Visibility="{Binding HasAttachments, Converter={x:Static views:BoolToVisibility.Instance}}">
                <StackPanel>
                    <Grid Margin="0,0,0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Style="{StaticResource InlineIconTextPanel}">
                            <TextBlock Text="&#xE723;"
                                       Style="{StaticResource InlineIconTextBlock.Small}"
                                       FontSize="11"
                                       Margin="0,0,4,0"
                                       Foreground="{DynamicResource Brush.Primary}" />
                            <TextBlock FontSize="11"
                                       Foreground="{DynamicResource Brush.Text.Secondary}"
                                       Style="{StaticResource InlineTextBlock.Small}">
                                <Run Text="已上传 " />
                                <Run Text="{Binding Attachments.Count, Mode=OneWay}"
                                     Foreground="{DynamicResource Brush.Primary}"
                                     FontWeight="SemiBold" />
                                <Run Text=" 个文件" />
                            </TextBlock>
                        </StackPanel>
                        <Button Grid.Column="1"
                                Background="Transparent"
                                BorderThickness="0"
                                Padding="6,2"
                                Cursor="Hand"
                                FontSize="11"
                                Foreground="{DynamicResource Brush.Text.Secondary}"
                                Command="{Binding ClearAttachmentsCommand}"
                                ToolTip="清空所有文件">
                            <StackPanel Style="{StaticResource InlineIconTextPanel}">
                                <TextBlock Text="&#xE74D;"
                                           Style="{StaticResource InlineIconTextBlock.Small}"
                                           FontSize="10"
                                           Margin="0,0,3,0" />
                                <TextBlock Text="清空" Style="{StaticResource InlineTextBlock.Small}" />
                            </StackPanel>
                        </Button>
                    </Grid>
                    <ItemsControl ItemsSource="{Binding Attachments}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{DynamicResource Brush.Bg.Secondary}"
                                        BorderBrush="{DynamicResource Brush.Border}"
                                        BorderThickness="1"
                                        CornerRadius="{DynamicResource Radius.Sm}"
                                        Padding="6,4"
                                        Margin="0,0,6,4"
                                        MaxWidth="180"
                                        MinWidth="96"
                                        MouseLeftButtonUp="OnAttachmentCardClick"
                                        ToolTip="{Binding FileName}">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsImage}" Value="True">
                                                    <Setter Property="Cursor" Value="Hand" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <!-- 32×32 thumbnail: image preview or file glyph -->
                                        <Border Width="32" Height="32"
                                                CornerRadius="3"
                                                Background="{DynamicResource Brush.Bg.Tertiary}"
                                                Margin="0,0,6,0"
                                                ClipToBounds="True">
                                            <Grid>
                                                <Image Source="{Binding Bytes, Converter={x:Static views:BytesToImageConverter.Instance}}"
                                                       Stretch="UniformToFill"
                                                       Visibility="{Binding IsImage, Converter={x:Static views:BoolToVisibility.Instance}}" />
                                                <TextBlock Text="&#xE7C3;"
                                                           FontFamily="{DynamicResource Font.Icon}"
                                                           FontSize="16"
                                                           Foreground="{DynamicResource Brush.Text.Secondary}"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"
                                                           Visibility="{Binding IsImage, Converter={x:Static views:BoolToVisibilityInverse.Instance}}" />
                                            </Grid>
                                        </Border>

                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding FileName, FallbackValue=附件}"
                                                   FontSize="11"
                                                   Foreground="{DynamicResource Brush.Text.Primary}"
                                                   VerticalAlignment="Center"
                                                   TextTrimming="CharacterEllipsis" />

                                        <Button Grid.Column="2"
                                                Margin="4,0,0,0"
                                                Width="18" Height="18"
                                                Padding="0"
                                                VerticalAlignment="Center"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Cursor="Hand"
                                                Opacity="0.55"
                                                ToolTip="移除"
                                                Click="OnRemoveAttachmentClick">
                                            <TextBlock Text="&#xE711;"
                                                       FontFamily="{DynamicResource Font.Icon}"
                                                       FontSize="10"
                                                       Foreground="{DynamicResource Brush.Text.Muted}" />
                                        </Button>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- Row 1: toolbar buttons. Attachment is always visible; thinking
                 and reasoning-effort controls depend on the active model; network
                 and webpage reading are exposed as separate toggles. -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,4">
                <!-- 角色 / Persona picker — BYOK mode only. Shows current persona's
                     avatar + name, click to open a popup with the full registry. -->
                <ToggleButton x:Name="PersonaToggle"
                              Style="{StaticResource ToolToggleButton}"
                              Visibility="{Binding IsPersonaPickerVisible, Converter={x:Static views:BoolToVisibility.Instance}}"
                              ToolTip="选择对话角色 / 系统提示词">
                    <StackPanel Style="{StaticResource InlineIconTextPanel}">
                        <TextBlock Text="{Binding Chat.ActivePersonaAvatar}"
                                   Style="{StaticResource InlineIconTextBlock.Small}"
                                   FontSize="13"
                                   Margin="0,0,6,0" />
                        <TextBlock Text="{Binding Chat.ActivePersonaName}"
                                   Style="{StaticResource InlineTextBlock.Small}" />
                    </StackPanel>
                </ToggleButton>
                <Popup x:Name="PersonaPopup"
                       IsOpen="{Binding IsChecked, ElementName=PersonaToggle}"
                       PlacementTarget="{Binding ElementName=PersonaToggle}"
                       Placement="Top"
                       StaysOpen="False"
                       AllowsTransparency="True"
                       PopupAnimation="Fade">
                    <Border Background="{DynamicResource Brush.Bg.Elevated}"
                            BorderBrush="{DynamicResource Brush.Border.Strong}"
                            BorderThickness="1"
                            CornerRadius="{DynamicResource Radius.Md}"
                            Padding="0"
                            MinWidth="320"
                            MaxWidth="420"
                            Margin="0,0,0,8">
                        <Border.Effect>
                            <DropShadowEffect Color="#33000000" BlurRadius="14" ShadowDepth="4" Opacity="0.35" />
                        </Border.Effect>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <!-- Header -->
                            <Grid Grid.Row="0" Margin="14,12,14,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="选择角色"
                                           FontWeight="SemiBold"
                                           FontSize="13"
                                           Foreground="{DynamicResource Brush.Text.Primary}"
                                           VerticalAlignment="Center" />
                                <Button Grid.Column="1"
                                        Click="OnClearPersonaClick"
                                        Style="{StaticResource ToolPlainButton}"
                                        Padding="8,2"
                                        ToolTip="清除当前对话的角色绑定，恢复默认助手"
                                        Visibility="{Binding Chat.ActivePersonaId, Converter={x:Static views:NullToInverseVisibility.Instance}}">
                                    <TextBlock Text="清除角色" FontSize="11" />
                                </Button>
                            </Grid>

                            <!-- Persona list -->
                            <ScrollViewer Grid.Row="1"
                                          VerticalScrollBarVisibility="Auto"
                                          HorizontalScrollBarVisibility="Disabled"
                                          MaxHeight="360"
                                          Padding="8,0,8,0">
                                <StackPanel>
                                    <Button Click="OnOpenImageWorkbenchClick"
                                            Cursor="Hand"
                                            HorizontalContentAlignment="Stretch"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            FocusVisualStyle="{x:Null}"
                                            Padding="0"
                                            Margin="0,1">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="row"
                                                        Background="Transparent"
                                                        CornerRadius="{DynamicResource Radius.Sm}"
                                                        Padding="10,8">
                                                    <ContentPresenter />
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="row" Property="Background"
                                                                Value="{DynamicResource Brush.Bg.HoverOverlay}" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0"
                                                       Text="&#xE91B;"
                                                       FontFamily="{DynamicResource Font.Icon}"
                                                       FontSize="16"
                                                       Width="28"
                                                       VerticalAlignment="Center"
                                                       TextAlignment="Center" />
                                            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                                <TextBlock Text="图像创建"
                                                           FontWeight="SemiBold"
                                                           FontSize="13"
                                                           Foreground="{DynamicResource Brush.Text.Primary}" />
                                                <TextBlock Text="打开图像工作台，继续生成和管理作品。"
                                                           FontSize="11"
                                                           Foreground="{DynamicResource Brush.Text.Muted}"
                                                           TextTrimming="CharacterEllipsis"
                                                           Margin="0,2,0,0" />
                                            </StackPanel>
                                            <TextBlock Grid.Column="2"
                                                       Text="&#xE72A;"
                                                       FontFamily="{DynamicResource Font.Icon}"
                                                       FontSize="13"
                                                       Foreground="{DynamicResource Brush.Text.Muted}"
                                                       VerticalAlignment="Center"
                                                       Margin="8,0,0,0" />
                                        </Grid>
                                    </Button>
                                    <Border Height="1"
                                            Margin="10,4"
                                            Background="{DynamicResource Brush.Divider}" />
                                    <ItemsControl ItemsSource="{Binding Personas.Personas}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                            <Button Click="OnPickPersonaClick"
                                                    Tag="{Binding Id}"
                                                    Cursor="Hand"
                                                    HorizontalContentAlignment="Stretch"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    FocusVisualStyle="{x:Null}"
                                                    Padding="0"
                                                    Margin="0,1">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border x:Name="row"
                                                                Background="Transparent"
                                                                CornerRadius="{DynamicResource Radius.Sm}"
                                                                Padding="10,8">
                                                            <ContentPresenter />
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="row" Property="Background"
                                                                        Value="{DynamicResource Brush.Bg.HoverOverlay}" />
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Button.Template>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0"
                                                               Text="{Binding DisplayAvatar}"
                                                               FontFamily="{DynamicResource Font.Icon}"
                                                               FontSize="16"
                                                               Width="28"
                                                               VerticalAlignment="Center"
                                                               TextAlignment="Center" />
                                                    <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                                        <TextBlock Text="{Binding Name}"
                                                                   FontWeight="SemiBold"
                                                                   FontSize="13"
                                                                   Foreground="{DynamicResource Brush.Text.Primary}"
                                                                   TextTrimming="CharacterEllipsis" />
                                                        <TextBlock Text="{Binding Preview}"
                                                                   FontSize="11"
                                                                   Foreground="{DynamicResource Brush.Text.Muted}"
                                                                   TextTrimming="CharacterEllipsis"
                                                                   Margin="0,2,0,0" />
                                                    </StackPanel>
                                                    <!-- Active checkmark -->
                                                    <TextBlock Grid.Column="2"
                                                               Text="&#xE73E;"
                                                               FontFamily="{DynamicResource Font.Icon}"
                                                               FontSize="14"
                                                               Foreground="{DynamicResource Brush.Primary}"
                                                               VerticalAlignment="Center"
                                                               Margin="6,0,0,0">
                                                        <TextBlock.Visibility>
                                                            <MultiBinding Converter="{x:Static views:EqualityToVisibility.Instance}">
                                                                <Binding Path="Id" />
                                                                <Binding Path="DataContext.Chat.ActivePersonaId"
                                                                         RelativeSource="{RelativeSource AncestorType=Popup}" />
                                                            </MultiBinding>
                                                        </TextBlock.Visibility>
                                                    </TextBlock>
                                                </Grid>
                                            </Button>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </ScrollViewer>

                            <!-- Footer: new + manage -->
                            <Border Grid.Row="2"
                                    BorderBrush="{DynamicResource Brush.Divider}"
                                    BorderThickness="0,1,0,0"
                                    Padding="10,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Button Grid.Column="0"
                                            Click="OnNewPersonaClick"
                                            Style="{StaticResource ToolPlainButton}"
                                            HorizontalAlignment="Left">
                                        <StackPanel Style="{StaticResource InlineIconTextPanel}">
                                            <TextBlock Text="&#xE710;"
                                                       Style="{StaticResource InlineIconTextBlock.Small}"
                                                       FontSize="12"
                                                       Margin="0,0,6,0" />
                                            <TextBlock Text="新建角色"
                                                       Style="{StaticResource InlineTextBlock.Small}" />
                                        </StackPanel>
                                    </Button>
                                    <Button Grid.Column="1"
                                            Click="OnManagePersonasClick"
                                            Style="{StaticResource ToolPlainButton}"
                                            HorizontalAlignment="Right">
                                        <TextBlock Text="管理…" />
                                    </Button>
                                </Grid>
                            </Border>
                        </Grid>
                    </Border>
                </Popup>

                <!-- 附件 -->
                <Button Style="{StaticResource ToolPlainButton}"
                        Click="OnAttachClick"
                        ToolTip="上传文件 / 图片"
                        Visibility="{Binding IsAttachVisible, Converter={x:Static views:BoolToVisibility.Instance}}">
                    <StackPanel Style="{StaticResource InlineIconTextPanel}">
                        <TextBlock Text="&#xE723;" Style="{StaticResource InlineIconTextBlock.Small}" FontSize="12" Margin="0,0,6,0" />
                        <TextBlock Text="附件" Style="{StaticResource InlineTextBlock.Small}" />
                    </StackPanel>
                </Button>

                <!-- 推理 (thinking toggle) — auto-hides on non-reasoning models -->
                <ToggleButton Style="{StaticResource ToolToggleButton}"
                              IsChecked="{Binding EnableThinking}"
                              ToolTip="开启推理：复杂问题先推理后回答"
                              Visibility="{Binding IsThinkingVisible, Converter={x:Static views:BoolToVisibility.Instance}}">
                    <StackPanel Style="{StaticResource InlineIconTextPanel}">
                        <!-- Lightbulb icon -->
                        <TextBlock Text="&#xE82F;" Style="{StaticResource InlineIconTextBlock.Small}" FontSize="12" Margin="0,0,6,0" />
                        <TextBlock Text="推理" Style="{StaticResource InlineTextBlock.Small}" />
                    </StackPanel>
                </ToggleButton>

                <!-- 推理强度 — popup adapts to ActiveThinkingKind -->
                <ToggleButton x:Name="EffortToggle"
                              Style="{StaticResource ToolToggleButton}"
                              ToolTip="调整推理强度"
                              Visibility="{Binding IsReasoningEffortVisible, Converter={x:Static views:BoolToVisibility.Instance}}">
                    <StackPanel Style="{StaticResource InlineIconTextPanel}">
                        <TextBlock Text="&#xF8B0;" Style="{StaticResource InlineIconTextBlock.Small}" FontSize="12" Margin="0,0,6,0" />
                        <TextBlock Style="{StaticResource InlineTextBlock.Small}">
                            <Run Text="强度: " />
                            <Run Text="{Binding ReasoningEffortLabel, Mode=OneWay}" />
                        </TextBlock>
                    </StackPanel>
                </ToggleButton>
                <Popup IsOpen="{Binding IsChecked, ElementName=EffortToggle}"
                       PlacementTarget="{Binding ElementName=EffortToggle}"
                       Placement="Top"
                       StaysOpen="False"
                       AllowsTransparency="True"
                       PopupAnimation="Fade">
                    <Border Background="{DynamicResource Brush.Bg.Elevated}"
                            BorderBrush="{DynamicResource Brush.Border.Strong}"
                            BorderThickness="1"
                            CornerRadius="{DynamicResource Radius.Md}"
                            Padding="12,10"
                            Margin="0,0,0,6">
                        <Border.Effect>
                            <DropShadowEffect Color="#33000000" BlurRadius="12" ShadowDepth="4" Opacity="0.3" />
                        </Border.Effect>
                        <StackPanel MinWidth="220">
                            <TextBlock Text="推理强度"
                                       FontWeight="SemiBold"
                                       FontSize="{DynamicResource Font.Size.Small}"
                                       Foreground="{DynamicResource Brush.Text.Primary}"
                                       Margin="0,0,0,8" />
                            <!-- Chip-style picker for reasoning effort:
                                 WrapPanel of selectable pills, each one
                                 maps a single value (low/medium/high/...). ListBox carries
                                 the SelectedItem ↔ ReasoningEffort binding; the
                                 ComposerViewModel normalizes the value when the model
                                 changes so the ListBox never silently writes null. -->
                            <ListBox x:Name="EffortChips"
                                     ItemsSource="{Binding AvailableEffortLevels}"
                                     SelectedItem="{Binding ReasoningEffort, Mode=TwoWay}"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     Padding="0"
                                     Margin="0,0,0,4"
                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                     ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                     Visibility="{Binding IsEffortComboVisible, Converter={x:Static views:BoolToVisibility.Instance}}">
                                <ListBox.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Horizontal" />
                                    </ItemsPanelTemplate>
                                </ListBox.ItemsPanel>
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="Cursor" Value="Hand" />
                                        <Setter Property="Margin" Value="0,0,6,6" />
                                        <Setter Property="Padding" Value="0" />
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ListBoxItem">
                                                    <Border x:Name="chipBorder"
                                                            Background="{DynamicResource Brush.Bg.Tertiary}"
                                                            BorderBrush="{DynamicResource Brush.Border}"
                                                            BorderThickness="1"
                                                            CornerRadius="{DynamicResource Radius.Sm}"
                                                            Padding="12,5">
                                                        <TextBlock x:Name="chipText"
                                                                   Text="{Binding Converter={x:Static views:EffortLabelConverter.Instance}}"
                                                                   FontSize="{DynamicResource Font.Size.Small}"
                                                                   Foreground="{DynamicResource Brush.Text.Secondary}" />
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="chipBorder" Property="BorderBrush" Value="{DynamicResource Brush.Primary.Border}" />
                                                            <Setter TargetName="chipText" Property="Foreground" Value="{DynamicResource Brush.Primary.Hover}" />
                                                        </Trigger>
                                                        <Trigger Property="IsSelected" Value="True">
                                                            <Setter TargetName="chipBorder" Property="Background" Value="{DynamicResource Brush.Primary.Soft}" />
                                                            <Setter TargetName="chipBorder" Property="BorderBrush" Value="{DynamicResource Brush.Primary}" />
                                                            <Setter TargetName="chipText" Property="Foreground" Value="{DynamicResource Brush.Primary}" />
                                                            <Setter TargetName="chipText" Property="FontWeight" Value="SemiBold" />
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                            </ListBox>
                            <StackPanel Visibility="{Binding IsBudgetSliderVisible, Converter={x:Static views:BoolToVisibility.Instance}}">
                                <Grid Margin="0,0,0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="Token Budget"
                                               FontSize="{DynamicResource Font.Size.Small}"
                                               Foreground="{DynamicResource Brush.Text.Secondary}" />
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding ThinkingBudgetTokens}"
                                               FontSize="{DynamicResource Font.Size.Small}"
                                               Foreground="{DynamicResource Brush.Text.Primary}" />
                                </Grid>
                                <Slider Minimum="{Binding BudgetMin}"
                                        Maximum="{Binding BudgetMax}"
                                        Value="{Binding ThinkingBudgetTokens, Mode=TwoWay}"
                                        TickFrequency="1024"
                                        IsSnapToTickEnabled="False"
                                        SmallChange="512"
                                        LargeChange="4096" />
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Popup>

                <!-- 联网搜索 -->
                <ToggleButton Style="{StaticResource ToolToggleButton}"
                              IsChecked="{Binding EnableNetwork}"
                              IsEnabled="{Binding AreNetworkToolsEnabled}"
                              ToolTip="联网搜索">
                    <StackPanel Style="{StaticResource InlineIconTextPanel}">
                        <!-- Search glyph -->
                        <TextBlock Text="&#xE721;" Style="{StaticResource InlineIconTextBlock.Small}" FontSize="12" Margin="0,0,6,0" />
                        <TextBlock Text="联网搜索" Style="{StaticResource InlineTextBlock.Small}" />
                    </StackPanel>
                </ToggleButton>

                <!-- 网页阅读 / web_fetch -->
                <ToggleButton Style="{StaticResource ToolToggleButton}"
                              IsChecked="{Binding EnableWebFetch}"
                              IsEnabled="{Binding AreNetworkToolsEnabled}"
                              ToolTip="网页阅读">
                    <StackPanel Style="{StaticResource InlineIconTextPanel}">
                        <TextBlock Text="&#xE774;" Style="{StaticResource InlineIconTextBlock.Small}" FontSize="12" Margin="0,0,6,0" />
                        <TextBlock Text="网页阅读" Style="{StaticResource InlineTextBlock.Small}" />
                    </StackPanel>
                </ToggleButton>

                <!-- BYOK 本地 Python Tool -->
                <ToggleButton Style="{StaticResource ToolToggleButton}"
                              IsChecked="{Binding EnablePythonTool}"
                              ToolTip="Python"
                              Visibility="{Binding IsPythonToolVisible, Converter={x:Static views:BoolToVisibility.Instance}}">
                    <StackPanel Style="{StaticResource InlineIconTextPanel}">
                        <TextBlock Text="&#xE943;" Style="{StaticResource InlineIconTextBlock.Small}" FontSize="12" Margin="0,0,6,0" />
                        <TextBlock Text="Python" Style="{StaticResource InlineTextBlock.Small}" />
                    </StackPanel>
                </ToggleButton>

            </StackPanel>

            <!-- Row 2: image generation options -->
            <Border Grid.Row="2"
                    Background="{DynamicResource Brush.Bg.Tertiary}"
                    BorderBrush="{DynamicResource Brush.Border}"
                    BorderThickness="1"
                    CornerRadius="{DynamicResource Radius.Sm}"
                    Padding="10,6"
                    Margin="0,0,0,6"
                    Visibility="{Binding IsImageOptionsVisible, Converter={x:Static views:BoolToVisibility.Instance}}">
                <WrapPanel Orientation="Horizontal">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,16,4">
                        <TextBlock Text="比例"
                                   FontSize="12"
                                   Foreground="{DynamicResource Brush.Text.Secondary}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0" />
                        <ListBox ItemsSource="{Binding ImageAspectRatioOptions}"
                                 SelectedValue="{Binding ImageAspectRatio, Mode=TwoWay}"
                                 SelectedValuePath="Value"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Padding="0"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 ScrollViewer.VerticalScrollBarVisibility="Disabled">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Cursor" Value="Hand" />
                                    <Setter Property="Margin" Value="0,0,4,0" />
                                    <Setter Property="Padding" Value="0" />
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <Border x:Name="chipBorder"
                                                        Background="Transparent"
                                                        BorderBrush="{DynamicResource Brush.Border}"
                                                        BorderThickness="1"
                                                        CornerRadius="10"
                                                        Padding="9,3">
                                                    <TextBlock x:Name="chipText"
                                                               Text="{Binding Label}"
                                                               FontSize="12"
                                                               Foreground="{DynamicResource Brush.Text.Secondary}" />
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="chipBorder" Property="BorderBrush" Value="{DynamicResource Brush.Primary.Border}" />
                                                        <Setter TargetName="chipText" Property="Foreground" Value="{DynamicResource Brush.Primary.Hover}" />
                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="chipBorder" Property="Background" Value="{DynamicResource Brush.Primary.Soft}" />
                                                        <Setter TargetName="chipBorder" Property="BorderBrush" Value="{DynamicResource Brush.Primary}" />
                                                        <Setter TargetName="chipText" Property="Foreground" Value="{DynamicResource Brush.Primary}" />
                                                        <Setter TargetName="chipText" Property="FontWeight" Value="SemiBold" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,4">
                        <TextBlock Text="风格"
                                   FontSize="12"
                                   Foreground="{DynamicResource Brush.Text.Secondary}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0" />
                        <ListBox ItemsSource="{Binding ImageStyleOptions}"
                                 SelectedValue="{Binding ImageStyle, Mode=TwoWay}"
                                 SelectedValuePath="Value"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Padding="0"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 ScrollViewer.VerticalScrollBarVisibility="Disabled">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Cursor" Value="Hand" />
                                    <Setter Property="Margin" Value="0,0,4,0" />
                                    <Setter Property="Padding" Value="0" />
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <Border x:Name="chipBorder"
                                                        Background="Transparent"
                                                        BorderBrush="{DynamicResource Brush.Border}"
                                                        BorderThickness="1"
                                                        CornerRadius="10"
                                                        Padding="9,3">
                                                    <TextBlock x:Name="chipText"
                                                               Text="{Binding Label}"
                                                               FontSize="12"
                                                               Foreground="{DynamicResource Brush.Text.Secondary}" />
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="chipBorder" Property="BorderBrush" Value="{DynamicResource Brush.Primary.Border}" />
                                                        <Setter TargetName="chipText" Property="Foreground" Value="{DynamicResource Brush.Primary.Hover}" />
                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="chipBorder" Property="Background" Value="{DynamicResource Brush.Primary.Soft}" />
                                                        <Setter TargetName="chipBorder" Property="BorderBrush" Value="{DynamicResource Brush.Primary}" />
                                                        <Setter TargetName="chipText" Property="Foreground" Value="{DynamicResource Brush.Primary}" />
                                                        <Setter TargetName="chipText" Property="FontWeight" Value="SemiBold" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>
                    </StackPanel>
                </WrapPanel>
            </Border>

            <!-- Row 3: textarea -->
            <Grid Grid.Row="3">
                <TextBlock Text="{Binding ComposerPlaceholder}"
                           Margin="6,4"
                           FontFamily="{DynamicResource Font.Cjk}"
                           FontSize="{DynamicResource Font.Size.BodyLg}"
                           Foreground="{DynamicResource Brush.Text.Muted}"
                           IsHitTestVisible="False"
                           Visibility="{Binding IsComposerPlaceholderVisible, Converter={x:Static views:BoolToVisibility.Instance}}" />
                <TextBox x:Name="InputBox"
                         Style="{StaticResource ComposerTextBox}"
                         Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}"
                         PreviewKeyDown="InputBox_PreviewKeyDown" />
            </Grid>

            <!-- Row 4: action bar (left hint, right send/stop) -->
            <Grid Grid.Row="4" Margin="0,6,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource Brush.Text.Muted}"
                           FontSize="{DynamicResource Font.Size.XSmall}"
                           Text="Enter 发送 · Shift+Enter 换行" />

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="SendButton"
                            Style="{StaticResource SendButton}"
                            Command="{Binding SendCommand}"
                            ToolTip="发送 (Ctrl+Enter)"
                            Visibility="{Binding IsSending,
                                                  Converter={x:Static views:BoolToVisibilityInverse.Instance}}">
                        <TextBlock Text="&#xE724;" FontFamily="{DynamicResource Font.Icon}" />
                    </Button>
                    <Button Style="{StaticResource StopButton}"
                            Command="{Binding StopCommand}"
                            ToolTip="停止"
                            Visibility="{Binding IsSending,
                                                  Converter={x:Static views:BoolToVisibility.Instance}}">
                        <TextBlock Text="&#xE71A;" FontFamily="{DynamicResource Font.Icon}" />
                    </Button>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>
